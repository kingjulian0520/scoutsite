// Fill these in with your real Stripe Payment Link URLs once you've created
// them in the Stripe Dashboard (Payment Links -> create one per tier).
// Leave a tier blank/empty to show the "not connected yet" notice for it.
const STRIPE_PAYMENT_LINKS = {
  basic: "",
  pro: "",
  elite: "",
};
